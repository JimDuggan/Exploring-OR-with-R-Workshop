# load the ggplot2 package
library(ggplot2)

# Show the mpg tibble
mpg

# Show the table using R Studio's View() function
View(mpg)

# Call the function summary() to see a summary of each variable
summary(mpg)
